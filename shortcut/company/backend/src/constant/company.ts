const jsonConfig = require('../../fuseble-config.json');

export const COMPANY_NAME = jsonConfig.company_name;

export const SERVICE_NAME = jsonConfig.service;

export const SERVICE_VERSION = jsonConfig.version;
